const header = () => {
    const getClub = document.querySelectorAll('.clubs-list>ul>li');

   getClub.addEventListener('charge').forEach((elem) => {
       let target = target.elem;
       console.log(target);
   });


}



export default header;